[metadata]
name = perm-framework
version = 0.1.0
description = Permutation test + bootstrap framework for BEN metrics

[options]
packages = find:
python_requires = >=3.9
install_requires =
    numpy
    pandas
    scipy
    matplotlib

